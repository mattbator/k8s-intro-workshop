apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  labels:
    app: lab-app
spec:
  storageClassName: "hostpath-csi"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi